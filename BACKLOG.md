# command-block backlog

- [ ] Deploy (build plan Part F): clone to `/opt/stacks/command-block`, write `.env`, `docker compose up -d --build`
- [ ] Run Part G acceptance tests against the real server (incl. the restart button)
- [ ] Homepage tile for command-block (`href: http://teamgreenserver:8300`)
- [ ] Uptime Kuma HTTP monitor on `http://<LAN-IP>:8300/healthz`
- [ ] network-reference bump: `:8300` = command-block
- [ ] Vaultwarden entry for the RCON password (now referenced by two `.env` files)
